{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "cpp_debug",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/a.out",
            "args": [
                ""
            ],
            "cwd": "${fileDirname}",
            "preLaunchTask": "cpp_build",
            "postDebugTask": "rm_file"
        }
    ]
}